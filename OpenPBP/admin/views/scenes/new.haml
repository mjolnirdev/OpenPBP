%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:scenes, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:scenes, :new)

.tabs-content
  = form_for :scene, url(:scenes, :create), :class => 'form-horizontal' do |f|
    =partial 'scenes/form', :locals => { :f => f }
